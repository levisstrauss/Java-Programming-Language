package AdvancedConcepts.Collections;

import java.util.HashSet;
import java.util.Set;

// No duplicate but not order but uniqueness
public class Sets {
    Set<String> set = new HashSet<>(/* we can pass a collection here */);
    //add()
}
