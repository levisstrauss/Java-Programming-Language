package AdvancedConcepts.Collections;

import java.util.ArrayList;
import java.util.List;

public class ListDemo {
    // For order use List
    List<String> list = new ArrayList<>();
    // add()
    // get(0)
    // set(0, 2)
    // remove(0)
    // indexOf(2)
    // lastIndexOf(2)
    //subList(0, 2) get the item between 0 and 2
}
